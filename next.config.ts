import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // experimental: {
  //   staleTimes: {
  //     dynamic: 30, // cache dynamic pages in client router for 30s
  //     static: 180, // cache static pages for 3 minutes
  //   },
  // },
};

export default nextConfig;
